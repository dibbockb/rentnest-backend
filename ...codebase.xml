This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
package.json
prisma.config.ts
prisma/schema/categories.prisma
prisma/schema/enums.prisma
prisma/schema/payments.prisma
prisma/schema/properties.prisma
prisma/schema/rental_requests.prisma
prisma/schema/reviews.prisma
prisma/schema/schema.prisma
prisma/schema/users.prisma
src/app.ts
src/config/envConfig.ts
src/lib/prisma.ts
src/server.ts
src/utils/globalErrorHandler.ts
src/utils/handleAsync.ts
src/utils/sendResponse.ts
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="prisma/schema/categories.prisma">
model Categories {
    id   String @id @unique @default(uuid())
    name String @unique

    properties Properties[]

    @@map("categories")
}
</file>

<file path="prisma/schema/payments.prisma">
model Payments {
    id                String        @id @unique @default(uuid())
    method            String
    provider          String
    rental_request_id String
    amount            Int
    status            PaymentStatus
    paid_at           DateTime

    rental_request Rental_Requests @relation(fields: [rental_request_id], references: [id], onDelete: Cascade)

    @@map("payments")
}
</file>

<file path="prisma/schema/properties.prisma">
model Properties {
    id           String   @id @unique @default(uuid())
    is_available Boolean  @default(true)
    location     String
    price        Int      @default(0)
    landlord_id  String
    category_id  String
    created_at   DateTime @default(now())
    updated_at   DateTime @updatedAt

    landlord User       @relation(fields: [landlord_id], references: [id], onDelete: Cascade)
    category Categories @relation(fields: [category_id], references: [id], onDelete: Restrict) ///toUnderstand

    rental_requests Rental_Requests[] ///toUnderstand
    reviews         Reviews[]

    @@map("properties")
}
</file>

<file path="prisma/schema/rental_requests.prisma">
model Rental_Requests {
    id           String              @id @unique @default(uuid())
    property_id  String
    requested_by String
    status       RentalRequestStatus @default(PENDING)
    is_muted     Boolean             @default(false)
    created_at   DateTime            @default(now())
    updated_at   DateTime            @updatedAt

    property Properties @relation(fields: [property_id], references: [id], onDelete: Cascade)
    tenant   User       @relation(fields: [requested_by], references: [id], onDelete: Cascade)
    payments Payments[]

    @@map("rental_requests")
}
</file>

<file path="prisma/schema/reviews.prisma">
model Reviews {
    id          String   @id @unique @default(uuid())
    property_id String
    tenant_id   String
    content     String   @db.Text
    rating      Int      @db.SmallInt
    created_at  DateTime @default(now())
    updated_at  DateTime @updatedAt

    tenant   User       @relation(fields: [tenant_id], references: [id], onDelete: Cascade)
    property Properties @relation(fields: [property_id], references: [id], onDelete: Cascade)

    @@map("reviews")
}
</file>

<file path="prisma.config.ts">
// This file was generated by Prisma, and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
    schema: "prisma/schema",
    migrations: {
        path: "prisma/migrations",
    },
    datasource: {
        url: process.env["DATABASE_URL"],
    },
});
</file>

<file path="prisma/schema/enums.prisma">
enum UserRoles {
    TENANT
    LANDLORD
    ADMIN
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}

// enum CategoriesList {
//     House
//     Apartment
//     Studio
//     Duplex
//     Tinyhouse
//     Penthouse
// }
</file>

<file path="prisma/schema/schema.prisma">
// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
    provider = "prisma-client"
    output   = "../../generated/prisma"
}

datasource db {
    provider = "postgresql"
}
</file>

<file path="prisma/schema/users.prisma">
model User {
    id         String    @id @unique @default(uuid())
    name       String    @db.VarChar(255)
    email      String    @unique @db.VarChar(255)
    password   String
    role       UserRoles @default(TENANT)
    is_banned  Boolean   @default(false)
    created_at DateTime  @default(now())
    updated_at DateTime  @updatedAt

    properties      Properties[]
    rental_requests Rental_Requests[]
    reviews         Reviews[]

    @@map("users")
}
</file>

<file path="src/app.ts">
import express, { Application, NextFunction, Request, Response } from "express";
import cors from "cors"
import envConfig from "./config/envConfig";

const app: Application = express()

app.use(cors({ origin: envConfig.app_url, credentials: true }))
app.use(express.json());

app.get(`/`, (req: Request, res: Response) => {
    res.send(`Server is Running...`)
})

export default app;
</file>

<file path="src/lib/prisma.ts">
import { Pool } from "pg";
import { PrismaPg } from "@prisma/adapter-pg";
import envConfig from "../config/envConfig";
import { PrismaClient } from "../../generated/prisma/client";

const pool = new Pool({ connectionString: envConfig.db_url });

const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

export { prisma };
</file>

<file path="src/utils/globalErrorHandler.ts">
import { NextFunction, Request, Response } from "express";
import status from "http-status"

export const globalErrorHandler = (err: any, req: Request, res: Response, next: NextFunction) => {
    const statusCode = err.statusCode || status.INTERNAL_SERVER_ERROR;

    res.status(statusCode).json({
        success: false,
        statusCode: statusCode,
        message: (err as Error).message || "Something went wrong.",
        error: err.stack
    })
}
</file>

<file path="src/utils/handleAsync.ts">
import { NextFunction, Request, RequestHandler, Response } from "express";

export const handleAsync = (fn: RequestHandler) => {
    return async (req: Request, res: Response, next: NextFunction) => {
        try {
            await fn(req, res, next)
        } catch (error) {
            next(error)
        }
    }
}
</file>

<file path="src/utils/sendResponse.ts">
import { Response } from "express"

type TMeta = {
    page: number,
    limit: number,
    total: number
}

type TResponseData<T> = {
    success: boolean,
    statusCode: number,
    message: string,
    data: T,
    meta?: TMeta
}

export const sendResponse = <T>(res: Response, data: TResponseData<T>) => {
    res.status(data.statusCode).json({
        success: data.success,
        statusCode: data.statusCode,
        message: data.message,
        data: data.data,
        meta: data.meta
    })
}
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "outDir": "./dist",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "target": "ES2023",
    "types": ["node"],
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,
    "noUncheckedIndexedAccess": true,
    "strict": true,
    "isolatedModules": true,
    "noUncheckedSideEffectImports": true,
    "moduleDetection": "force",
    "skipLibCheck": true
  },
  // "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
</file>

<file path="src/config/envConfig.ts">
import dotenv from "dotenv"
import path from "path"

dotenv.config({
    path: path.join(process.cwd(), ".env")
})

export default {
    app_url: process.env.APP_URL,
    port: process.env.PORT || 5001,
    db_url: process.env.DATABASE_URL
}
</file>

<file path="src/server.ts">
import app from "./app";
import envConfig from "./config/envConfig";
import { prisma } from "./lib/prisma";

const port = envConfig.port;

async function main() {
    try {
        await prisma.$connect();
        console.log(`Connected to Database...`)

        app.listen(port, () => {
            console.log(`Server is running on port ::: ${port}`);
        })
    }
    catch (error) {
        await prisma.$disconnect();
        console.log(`Error starting the server::: ${error}`)
        process.exit(1);
    }
}

main()
</file>

<file path=".gitignore">
node_modules
dist
.env
generated
others
</file>

<file path="package.json">
{
  "name": "4-rentnest-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.10.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "http-status": "^2.1.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0",
    "stripe": "^22.3.0"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/http-status": "^1.1.2",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
</file>

</files>
